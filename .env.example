# This root file documents environment variables used by QueryLens.
# Do not place real secrets in this file.
# Copy relevant values into environment-specific .env files when needed.

# Backend
SERVER_PORT=8080
ALLOWED_ORIGINS=http://localhost:5173

# Frontend
VITE_API_BASE_URL=http://localhost:8080
